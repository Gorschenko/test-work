const SERVER_PROTOCOL = 'http';
const SERVER_HOSTNAME = 'localhost';
const SERVER_PORT = 80;
export const SERVER_BASE_URL = `${SERVER_PROTOCOL}://${SERVER_HOSTNAME}:${SERVER_PORT}`;

export const PATHS = {
  HOME: 'home',
  ADMIN: 'admin',
  UNAUTHORIZED: 'unauthorized',
  FORBIDEEN: 'fobidden',
  NOT_FOUND: 'not_found',
  CONFLICT: 'conflict',
};
